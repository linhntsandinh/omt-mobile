import {connect} from 'react-redux';
import CreateUserPage1 from "../View/CreateUserPage1";

const mapStateToProps = (state) => {
    return {

    }
};

const mapDispatchToProps = (dispatch) => {
    return {

    };
}

export default connect(mapStateToProps, mapDispatchToProps)(CreateUserPage1);
